Currently skeleton server setup for a student project.
Only db scheme sql setup available now.

Requirements:

go version 1.20

Back-end Setup:

`go build` and run the binary

Front-end Setup:

`npm install` and `npm start`